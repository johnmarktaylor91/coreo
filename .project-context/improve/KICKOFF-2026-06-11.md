# Coreo Full-Scope Improvement Run -- Kickoff Brief

**Authorized:** JMT, 2026-06-11 18:16 EDT (this session). Scheduled kickoff: 19:01 EDT.
**Repo:** /Users/jmt/projects/coreo (branch: main)
**Run name:** full-scope-improve

## Authorization & Scope (JMT's exact directives)

- "FULL SCOPE for improving the app, everything on the table."
- Explicit model override: "dispatch several parallel fables" for the survey phase.
- **FULL OVERRIDE confirmed** on project guardrails: data-model shape changes
  (Project/VideoClip/Annotation) and sync-algorithm design changes ARE allowed when
  the agent is confident it's an improvement. (Normally protected by project CLAUDE.md;
  JMT overrode 2026-06-11.)
- End state: **merge to main when green**, delete working branch.
- Survey covers: performance, efficiency, robustness, edge cases, smoothness,
  responsiveness, UI/UX feedback -- "open-endedly and exhaustively cover everything."
- Use taste to improve UI/feedback; table ONLY what genuinely requires human signoff
  (on-device aesthetic judgment, App Store/signing/provisioning, StoreKit/IAP).
- After surveys: dispatch Codexes to implement, then review, verify, commit.
- **Text JMT when done** via ~/.claude/scripts/send-to-jmt.sh.

## Phase 0 -- Preflight (do FIRST on kickoff)

1. `pkill -f 'codex-watch\.sh|bg-watch\.sh' 2>/dev/null || true`
2. Check pause sentinels: `ls ~/.claude/state/paused-*.sentinel 2>/dev/null`.
   Codex paused -> survey proceeds (Claude-side), but STOP before Phase 3 and ask JMT.
   Claude paused -> stop, ask JMT.
3. Resource check: `df -h /Users` (need >20GB for simulator builds) and `uptime`.
4. **Baseline commit:** the entire app source (Coreo/, CoreoTests/, Coreo.xcodeproj/,
   project.yml, DESIGN.md, EDGE-CASES.md, PERFORMANCE.md, UI-POLISH.md, modified
   .project-context/ files) is currently UNCOMMITTED -- only "Initial commit" exists.
   Commit ALL current work to main as a baseline (conventional commit, e.g.
   `feat: baseline app implementation pre improvement run`). NO AI attribution of any
   kind (no Co-Authored-By Claude/Happy, no Generated-with footer -- JMT global rule).
5. Record known-red tests: run Tier 1 (`xcodebuild test -scheme Coreo -destination
   'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:CoreoTests/UnitTests | xcbeautify`)
   and save the pass/fail baseline to .project-context/improve/baseline-tests.md.
6. Create state file `.project-context/improve/full-scope-improve_STATE.md` (template
   at bottom of this brief) BEFORE any multi-round dispatching.

## Phase 1 -- Fable Survey Squad (parallel, read-only)

Dispatch 6 parallel subagents via Agent tool, **model: "fable"** (explicit JMT override).
All in ONE response. Each is read-only (subagent_type Explore or general-purpose with
read-only instruction), surveys the whole codebase through its lens, and writes a
structured report to `.project-context/improve/reports/<area>.md`.

Report contract (every agent): findings ordered by severity/impact; each finding has
file:line refs, concrete recommended change, blast radius (files touched), suggested
verification/test, and a confidence level. Distinguish inference from verified fact.
End with a prioritized top-10 list. Also read DESIGN.md, EDGE-CASES.md, PERFORMANCE.md,
UI-POLISH.md for prior context (they live at repo root).

The 6 lenses (collectively exhaustive; overlap is fine -- disagreement is signal):

1. **performance-efficiency** -- playback performance, multi-AVPlayer coordination,
   FFT sync engine (Sync/AudioSyncEngine.swift) algorithmic efficiency and Accelerate
   usage, memory footprint (2-6 simultaneous videos), thumbnail generation, startup
   time, energy. Report: reports/performance-efficiency.md
2. **robustness-edge-cases** -- error handling everywhere, import failures, weird
   media (codecs, HDR, rotation metadata, variable frame rate, missing/quiet audio,
   very long/short clips), 2-vs-6 video counts, interruptions (calls, backgrounding,
   audio session loss), permission denials, low disk, project persistence corruption.
   Report: reports/robustness-edge-cases.md
3. **concurrency-correctness** -- Swift 6 concurrency (actor isolation, Sendable, data
   races), WorkspaceViewModel multi-player coordination, timeline math correctness,
   sync drift over long playback, seek/scrub races, export-while-editing hazards.
   Report: reports/concurrency-correctness.md
4. **ui-ux-responsiveness** -- main-thread hitches, gesture latency, scrub
   responsiveness, loading/progress states, haptics, animation polish, feedback
   affordances, empty/error states, accessibility basics. Taste-based UI improvements
   ARE in scope; table only judgments that truly need JMT's eyes on a device.
   Report: reports/ui-ux-responsiveness.md
5. **architecture-quality-tests** -- module boundaries, SwiftUI patterns, dead code,
   oversized files, testability, test coverage gaps (CoreoTests/), API design between
   modules, persistence design. Data-model reshaping IS allowed (full override).
   Report: reports/architecture-quality-tests.md
6. **export-pipeline** -- Export/ExportEngine.swift AVMutableComposition correctness
   and performance, annotation compositor fidelity vs on-screen rendering, speed/hold
   segment handling, color/rotation/HDR handling, audio mix, end bumper, cancellation,
   progress reporting. Report: reports/export-pipeline.md

## Phase 2 -- Synthesis (main thread, me)

1. Read all 6 reports. Dedupe, resolve conflicts, prioritize by impact/risk.
2. Write `.project-context/improve/PLAN.md`: ordered improvement list with rationale.
3. Write `.project-context/improve/TABLED.md`: items needing JMT (on-device aesthetics,
   App Store/StoreKit, anything irreversible/uncertain).
4. Partition into Codex task specs with **disjoint write targets** (pre-dispatch
   checklist: shared file -> serialize into same spec or sequence waves). Specs are
   self-contained, gpt-5-4-prompting XML (<task>, <completeness_contract>,
   <verification_loop>, <action_safety>, <default_follow_through_policy>), include
   file:line, pasted snippets, quality gates, known-red baseline, AGENTS.md ref.
   Save specs to /tmp/PROMPT_coreo_<spec-name>.md AND list them in the state file.

## Phase 3 -- Codex Implementation

- Branch: `codex/full-scope-improvements` off main. ONE working branch total.
- Dispatch per spec: `~/.claude/scripts/codex-bg.sh /tmp/coreo_<spec>.log
  /tmp/PROMPT_coreo_<spec>.md --cd /Users/jmt/projects/coreo --sandbox
  danger-full-access -c model_reasoning_effort=medium`
- SAME response: Monitor on `~/.claude/scripts/codex-watch.sh <PID> /tmp/coreo_<spec>.log`.
  Then end turn briefly. On CODEX_DONE: verify (git log, gates), report, next wave.
- Parallel codexes ONLY with disjoint write targets; otherwise sequential waves.
- Quality gates per task: swiftlint; swiftformat --lint .; xcodebuild test -scheme
  Coreo -destination 'platform=iOS Simulator,name=iPhone 17 Pro' | xcbeautify
- Codex quota exhausted -> fallback: Claude subagents (sonnet mechanical / opus gnarly)
  per CLAUDE.md quota-fallback. Both blocked -> iMessage JMT, mark BLOCKED in state file.

## Phase 4 -- Review & Verify (main thread, me)

- Read diffs (not full files) per landed spec. Check against its report findings.
- Full Tier 3 gate: swiftlint && swiftformat --lint . && xcodebuild test (full suite).
- Issues found -> ONE focused fixup spec per task max; structural problems -> document,
  decide accept/defer; 3 rounds stuck on same issue -> accept residual, log it.

## Phase 5 -- Land & Notify

1. All gates green -> merge `codex/full-scope-improvements` into main (ff or merge),
   delete the branch. Conventional commits, human attribution only.
2. Write `.project-context/improve/SUMMARY.md`: what changed (by area), gates status,
   residuals, tabled items.
3. iMessage JMT: `~/.claude/scripts/send-to-jmt.sh "<concise summary: N improvements
   merged to main across X areas; gates green; tabled-for-you list; where to read more>"`
4. Update .project-context/knowledge/ (gotchas/decisions) with anything learned.

## Stop criteria (quantitative)

- All synthesized specs either landed+verified or 3-strike residual documented.
- Tier 3 quality gates green on main (or baseline-red tests no worse, documented).
- SUMMARY.md + TABLED.md written; iMessage sent. Then DONE.

## State file template (create as full-scope-improve_STATE.md)

```
# full-scope-improve STATE
round: 0  phase: preflight
## Case routing (read FIRST every wake-up; check git log -3, pgrep, log sizes)
A: codex done + committed -> verify gates, dispatch next wave
B: codex still running -> ack, yield turn
C: codex quota/fail -> fallback chain (sonnet/opus subagent), log it
D: tests failed -> diagnose root cause, one fixup spec, max 3 rounds/issue
## Stop criteria: (copy from brief)
## Specs: (fill in Phase 2)
## Iteration log: (append every wake-up)
```
